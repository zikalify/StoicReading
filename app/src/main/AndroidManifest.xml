<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.reading.stoic.stoicreading">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RufusHome" />
        <activity android:name=".SenecaHome" />
        <activity android:name=".EpictetusHome" />
        <activity android:name=".AureliusHome" />
        <activity android:name=".HieroclesHome" />
        <activity android:name=".RufusLectures.Lec1"></activity>
    </application>

</manifest>